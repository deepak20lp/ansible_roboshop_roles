- name: configuting the roboshop using ansible role
  hosts: "{{component}}"
  become: yes
  roles:
  - "{{component}}"



# - name: configuting the roboshop using ansible role
#   hosts: "{{component}}"
#   become: yes
#   roles:
#     - mongodb
#     - cart